package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Sophisticated Dark Premium Palette
val ObsidianBg = Color(0xFF0D0D0D) // Pure premium dark canvas
val SlateCard = Color(0xFF1C1B1F) // Surface depth card
val CharcoalCard = Color(0xFF28272C) // Slightly elevated gray card
val BorderStroke = Color(0xFF2E2C33) // Subtle, premium borders

// Branded Identity & Accent Highlights
val SophisticatedRose = Color(0xFFF2B8B5) // Warm rose gold heading & main CTA
val SophisticatedViolet = Color(0xFFD0BCFF) // Glowing lavender details/actions
val BurgundyBg = Color(0xFF31111D) // Velvet wine premium status badge
val BurgundyText = Color(0xFF601410) // Text to overlay on rose

val TextPrimary = Color(0xFFE6E1E5) // Clean modern gray-white
val TextSecondary = Color(0x99FFFFFF) // 60% transparent white
val TextMuted = Color(0x66FFFFFF) // 40% transparent white tracking label
